import React from "react";

type Props = {
  children: React.ReactNode;
  type?: "FREE" | "PRO";
};

export const SubsriptionPlan = ({ children, type }: Props) => {
  // return

  return <>{children}</>;
};
